version: "2.0"

intents:
  - yes
  - no
  - help
  - asking_ai_info
entities:
  - location
  - service
  - covid_19
session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
